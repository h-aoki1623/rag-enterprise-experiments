# /pr — Create a GitHub Pull Request (MUST)

Create a GitHub Pull Request for the current branch using `gh pr create`.
MUST NOT run on `main`; ensure all changes are committed and pushed.
If title/body are not provided, generate a concise PR title and body and output the PR URL.